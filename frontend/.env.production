# ─── Production (loaded by Next.js when NODE_ENV=production during build) ───
# Update these subdomains after setting up Cloudflare tunnel
NEXT_PUBLIC_API_URL=https://api.stonemark.pt
NEXT_PUBLIC_WS_URL=wss://api.stonemark.pt
